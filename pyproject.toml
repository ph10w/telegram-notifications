[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "telegram-voice-forwarder"
version = "0.1.0"
description = "Forward Telegram voice messages and send general Telegram notifications."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "python-dotenv>=1.0,<2",
  "Telethon>=1.44,<2",
]

[project.scripts]
tg-forwarder = "tg_forwarder.cli:main"
tg-notification = "tg_notification.cli:main"
tg-setup = "tg_setup.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
